auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false

# Skip the pre-script "install status" check that fails when optional
# native builds (sharp, unrs-resolver) haven't been rebuilt.
verify-deps-before-run=false
