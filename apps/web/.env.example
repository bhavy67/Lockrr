# Data mode. Set to "mock" (default) for local-only development,
# or "supabase" once you have a Supabase project (Phase 6+).
NEXT_PUBLIC_DATA_MODE=mock

# Only required when NEXT_PUBLIC_DATA_MODE=supabase
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
